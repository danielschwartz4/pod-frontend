import { addDays } from "./utils/singleTaskUtils";

export const ADD_TASKS_LIMIT = 28;
export const SKELETON_UNIT_SIZE = 7;
export const TODAY = new Date();
// export const TODAY = addDays(20, new Date());
