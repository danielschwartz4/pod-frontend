mutation AddProjectInfo($projectOptions: ProjectInput!) {
  addProjectInfo(projectOptions: $projectOptions) {
    project {
      id
      createdAt
      updatedAt
      milestoneDates
      milestones
      overview
      podId
      userId
      groupSize
    }
  }
}
