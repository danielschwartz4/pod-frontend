mutation UpdateProjectFriendRequests(
  $friendRequests: [String!]!
  $updateProjectFriendRequestsId: Float!
) {
  updateProjectFriendRequests(
    friendRequests: $friendRequests
    id: $updateProjectFriendRequestsId
  ) {
    errors
    project {
      ...RegularProject
    }
  }
}
