mutation UpdateProjectGroupSize(
  $groupSize: Float!
  $updateProjectGroupSizeId: Float!
) {
  updateProjectGroupSize(groupSize: $groupSize, id: $updateProjectGroupSizeId) {
    errors
    project {
      ...RegularProject
    }
  }
}
