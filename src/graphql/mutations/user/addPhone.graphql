mutation UpdatePhone($phone: String!, $updatePhoneId: Float!) {
  updatePhone(phone: $phone, id: $updatePhoneId) {
    errors {
      field
      message
    }
    user {
      ...RegularUser
    }
  }
}
