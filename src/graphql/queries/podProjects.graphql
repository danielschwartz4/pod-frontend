query PodProjects($podId: Int!) {
  podProjects(podId: $podId) {
    userId
    id
    milestoneDates
    milestones
    milestoneProgress
    groupSize
    createdAt
    updatedAt
    overview
    podId
    projectName
  }
}
