query Project($id: Int!) {
  project(id: $id) {
    errors
    project {
      userId
      id
      milestoneDates
      milestones
      milestoneProgress
      groupSize
      createdAt
      updatedAt
      overview
      podId
      projectName
    }
  }
}
