query Project($id: Int!) {
  project(id: $id) {
    project {
      userId
      id
      milestoneDates
      milestones
      groupSize
      createdAt
      updatedAt
      overview
      podId
      projectName
    }
  }
}
