query RecentPodSingleTasks($taskIds: [Int!]!) {
  recentPodSingleTasks(taskIds: $taskIds) {
    errors
    singleTasks {
      user {
        ...RegularUser
      }
      ...RegularSingleTask
    }
  }
}
